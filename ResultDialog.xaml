<Window x:Class="TemplateGenerator.ResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateGenerator"
        mc:Ignorable="d"
        Title="ResultDialog" Height="340" Width="492" ResizeMode="NoResize">
    <Grid>
        <TextBox
            x:Name="ResultBox"
            TextWrapping="Wrap"
            Margin="10,10,10,49"
            IsReadOnly="True"
        />
        <Grid Background="#3F000000" Margin="0,258,0,0">
            <Button
                x:Name="CloseDialog"
                Content="Close"
                HorizontalAlignment="Left"
                Margin="340,10,0,0"
                VerticalAlignment="Top"
                Height="24"
                Width="131" Click="CloseDialog_OnClick" />
            <Button
                x:Name="CopyToClipboard"
                Content="Copy to clipboard"
                HorizontalAlignment="Left"
                Margin="204,10,0,0"
                VerticalAlignment="Top"
                Height="24"
                Width="131"
                Click="CopyToClipboard_OnClick"
            />
        </Grid>
    </Grid>
</Window>
